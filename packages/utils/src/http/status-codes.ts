/**
 * HTTP Status Codes
 *
 * Re-exports from @auth/contracts for backward compatibility.
 * New code should import directly from @auth/contracts.
 */

export {
    HTTP_STATUS_CODES,
    isHttpStatusCode,
    type HttpStatusCode,
} from "@auth/contracts";
